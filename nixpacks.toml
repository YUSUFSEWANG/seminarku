[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction --ignore-platform-reqs",
    "chmod -R 777 storage bootstrap/cache"
]

[start]
cmd = "cp .env.example .env && chmod -R 777 storage bootstrap/cache && php artisan serve --host=0.0.0.0 --port=$PORT"
